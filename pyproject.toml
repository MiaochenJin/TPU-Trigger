[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "tpu-trigger"
version = "0.1.0"
description = "Trigger-level processing for neutrino telescopes on Coral Edge TPUs"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
